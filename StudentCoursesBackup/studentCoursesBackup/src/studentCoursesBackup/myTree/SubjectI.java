package studentCoursesBackup.myTree;

public interface SubjectI {
	/**
	* notifyAll method which is called from subject node to notifies the observer nodes.
	* @param
	*/
	public void notifyAll(int Bnumber,String course);
}
